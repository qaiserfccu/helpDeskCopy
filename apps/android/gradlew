#!/bin/sh

##############################################################################
##
##  Gradle start up script for UN*X
##
##############################################################################

# Note: This is a placeholder script. For building the Android app:
# 1. Open the project in Android Studio, or
# 2. Run: gradle wrapper --gradle-version=8.5 to generate proper wrapper files
# 3. The gradle-wrapper.jar and gradlew script will be generated automatically

echo "Please generate Gradle wrapper by opening this project in Android Studio"
echo "or by running: gradle wrapper --gradle-version=8.5"
echo ""
echo "Once generated, re-run: ./gradlew $@"
exit 1
